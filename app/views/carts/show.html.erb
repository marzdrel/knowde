<main class="content">
  <div class="container-fluid p-0">
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-body">

            <div class="row">
              <div class="col-sm-12 text-right">
                <%= link_to root_path, class: "btn btn-primary" do %>
                  Exit
                <% end %>
              </div>
            </div>

            <hr>

            <%= @action.message %>

            <div class="row">
              <div class="col-sm-12">
                <%= form_with model: @action.cart, data: { turbo: false } do |f| %>
                  <div class="mb-3">
                    <%= f.label :code, class: "form-label" do %> Enter barcode <% end %>
                    <%= f.text_field :code, class: "form-control", placeholder: "Type barcode, ex: 123", autofocus: true %>
                  </div>

                  <div class="mt-3">
                    <button type="submit" class="btn btn-primary">Scan</button>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>